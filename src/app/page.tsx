import Sidebar from "@/components/sidebar/sidebar";

const Homepage = () => {
  return (
    <main className="flex h-screen min-w-full px-24">
      <Sidebar />
    </main>
  );
};

export default Homepage;
